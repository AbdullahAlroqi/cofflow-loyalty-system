# تعليمات تثبيت وفحص PWA

## متطلبات تثبيت التطبيق كـ PWA

لكي يعمل تثبيت التطبيق كـ PWA (Progressive Web App) بشكل صحيح، يجب توفر الشروط التالية:

1. **خادم HTTPS**: 
   - أهم متطلبات PWA هو تشغيل الموقع عبر HTTPS
   - لن يعمل التثبيت من HTTP العادي (وهذا هو السبب الرئيسي لعدم ظهور زر التثبيت في متصفح كروم)
   - الاستثناء الوحيد هو localhost للتطوير المحلي

2. **متصفح حديث يدعم PWA**:
   - كروم: الإصدار 76 وما فوق
   - فايرفوكس: الإصدار 85 وما فوق
   - إيدج (Chromium): الإصدار 79 وما فوق
   - سفاري: iOS 16.4 وما فوق

## خطوات التثبيت المحلي للاختبار

إذا كنت ترغب في تجربة التثبيت محلياً، يمكنك اتباع هذه الخطوات:

1. **تشغيل التطبيق عبر HTTPS محلي**:
   - استخدم شهادة SSL محلية مع Flask
   - يمكنك استخدام أداة مثل mkcert لإنشاء شهادة SSL محلية موثوقة
   - أو استخدم خدمة مثل ngrok للحصول على عنوان HTTPS موجه للتطوير المحلي

2. **تشغيل التطبيق على HTTPS**:
   ```python
   if __name__ == '__main__':
       # للتطوير المحلي مع HTTPS
       app.run(debug=True, host='0.0.0.0', port=5000, ssl_context=('cert.pem', 'key.pem'))
       # أو استخدام مسار لمجلد الشهادات
       # app.run(debug=True, host='0.0.0.0', port=5000, ssl_context=('./ssl/cert.pem', './ssl/key.pem'))
   ```

3. **الوصول للتطبيق واختبار التثبيت**:
   - افتح الموقع في متصفح كروم بعنوان HTTPS
   - ستظهر أيقونة التثبيت في شريط العنوان
   - أو اضغط على زر التثبيت الموجود في الموقع

## فحص PWA

للتأكد من أن PWA يعمل بشكل صحيح، يمكنك اتباع هذه الخطوات:

1. **فتح أدوات المطور في كروم**:
   - اضغط F12 أو انقر بزر الماوس الأيمن واختر "فحص العنصر"
   - انتقل إلى علامة التبويب "Application"
   - من القائمة الجانبية، اختر "Service Workers"
   - تأكد من وجود Service Worker مسجل ونشط

2. **فحص Manifest**:
   - من نفس القائمة الجانبية، اختر "Manifest"
   - تأكد من أن جميع حقول Manifest تظهر بشكل صحيح
   - يجب أن ترى الأيقونات والألوان والإعدادات الأخرى

3. **اختبار العمل بدون اتصال**:
   - من علامة التبويب "Network"، اختر "Offline"
   - حاول تصفح الموقع للتأكد من أنه يعمل بدون اتصال
   - يجب أن ترى صفحة offline.html إذا حاولت الوصول لمحتوى غير مخزن

## نشر الموقع كـ PWA

عند نشر الموقع على الإنترنت، تأكد من:

1. استخدام HTTPS (ضروري لعمل PWA)
2. تأكد من أن جميع المسارات في ملف manifest.json و Service Worker صحيحة
3. استخدم أداة [Lighthouse](https://developers.google.com/web/tools/lighthouse) من جوجل لتقييم PWA الخاص بك

## استكشاف الأخطاء وإصلاحها

إذا لم يظهر زر التثبيت:

1. تأكد من استخدام HTTPS
2. افحص وحدة تحكم المتصفح (F12) للأخطاء
3. تأكد من تسجيل Service Worker بنجاح
4. تحقق من صحة ملف manifest.json
5. جرب استخدام متصفح كروم على جهاز Android للتثبيت (دعم أفضل)

---

**ملاحظة مهمة**: إذا كنت تستخدم التطبيق على خادم محلي (127.0.0.1 أو localhost) بدون HTTPS، فقد لا يظهر زر التثبيت. هذا سلوك طبيعي لمتصفح كروم. عند نشر التطبيق على خادم حقيقي مع HTTPS، ستعمل جميع ميزات PWA بشكل صحيح.
